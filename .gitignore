.env
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# generated by ingest.py - not source, don't commit
jobs.db

# monitoring log (created by db_init.py, grows as the app is used) - local data, not source
monitoring.db

# SQLite WAL-mode sidecar files (temporary, created alongside any .db file while it's in use)
*.db-shm
*.db-wal

# BigQuery service account key - secret, never commit
massive-bliss-481811-d8-31685420abbe.json

# regenerated by ingest.py each run - not source, don't commit (the eval
# CSVs in data/ are kept and pushed, just not these two)
data/vector_embeddings.npy
data/vector_documents.json
